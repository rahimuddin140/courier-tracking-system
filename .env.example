# Server Port
PORT=4000

# Session Secret Key
SESSION_SECRET=super-secret-courier-delivery-key-2026

# MongoDB Atlas Connection URI
# Replace <username>, <password>, and cluster domain with your MongoDB Atlas credentials:
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/courierDB?retryWrites=true&w=majority
