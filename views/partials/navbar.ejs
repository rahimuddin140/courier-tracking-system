<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <i class="bi bi-box2-heart-fill text-primary me-2 fs-4"></i>
      <span>FastTrack Logistics</span>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarMain">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= currentPath === '/track' ? 'active text-primary fw-semibold' : '' %>" href="/track">
            <i class="bi bi-search me-1"></i> Track Parcel
          </a>
        </li>

        <% if (currentUser) { %>
          <% if (currentUser.role === 'customer') { %>
            <li class="nav-item">
              <a class="nav-link <%= currentPath === '/customer/dashboard' ? 'active text-primary fw-semibold' : '' %>" href="/customer/dashboard">
                <i class="bi bi-speedometer2 me-1"></i> Dashboard
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= currentPath === '/customer/book' ? 'active text-primary fw-semibold' : '' %>" href="/customer/book">
                <i class="bi bi-plus-circle me-1"></i> Book Parcel
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= currentPath === '/customer/parcels' ? 'active text-primary fw-semibold' : '' %>" href="/customer/parcels">
                <i class="bi bi-boxes me-1"></i> My Parcels
              </a>
            </li>
          <% } else if (currentUser.role === 'agent') { %>
            <li class="nav-item">
              <a class="nav-link <%= currentPath === '/agent/dashboard' ? 'active text-primary fw-semibold' : '' %>" href="/agent/dashboard">
                <i class="bi bi-speedometer2 me-1"></i> Dashboard
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= currentPath === '/agent/parcels' ? 'active text-primary fw-semibold' : '' %>" href="/agent/parcels">
                <i class="bi bi-card-checklist me-1"></i> Assigned Parcels
              </a>
            </li>
          <% } else if (currentUser.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link <%= currentPath === '/admin/dashboard' ? 'active text-primary fw-semibold' : '' %>" href="/admin/dashboard">
                <i class="bi bi-graph-up-arrow me-1"></i> Dashboard
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= currentPath.startsWith('/admin/parcels') ? 'active text-primary fw-semibold' : '' %>" href="/admin/parcels">
                <i class="bi bi-box-seam me-1"></i> Parcels
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= currentPath.startsWith('/admin/agents') ? 'active text-primary fw-semibold' : '' %>" href="/admin/agents">
                <i class="bi bi-people me-1"></i> Delivery Agents
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= currentPath.startsWith('/admin/zones') ? 'active text-primary fw-semibold' : '' %>" href="/admin/zones">
                <i class="bi bi-geo-alt me-1"></i> Delivery Zones
              </a>
            </li>
          <% } %>
        <% } %>
      </ul>

      <ul class="navbar-nav ms-auto align-items-lg-center">
        <% if (currentUser) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <div class="bg-primary text-white rounded-circle d-inline-flex justify-content-center align-items-center me-2" style="width: 32px; height: 32px; font-weight: 600;">
                <%= currentUser.name.charAt(0).toUpperCase() %>
              </div>
              <span><%= currentUser.name %></span>
              <span class="badge bg-secondary ms-2 text-capitalize"><%= currentUser.role %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userDropdown">
              <li>
                <div class="dropdown-item-text text-muted small">
                  Signed in as<br><strong><%= currentUser.email %></strong>
                </div>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <a class="dropdown-item text-danger" href="/logout">
                  <i class="bi bi-box-arrow-right me-2"></i> Logout
                </a>
              </li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/login' ? 'active' : '' %>" href="/login">
              <i class="bi bi-box-arrow-in-right me-1"></i> Login
            </a>
          </li>
          <li class="nav-item ms-lg-2">
            <a class="btn btn-primary btn-sm px-3" href="/register">
              <i class="bi bi-person-plus me-1"></i> Register
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
